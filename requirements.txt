# ── Build tools (host, not bundled into APK) ──────────────────
Cython==0.29.33
buildozer>=1.5.0

# ── Core Kivy stack (versions that have p4a recipes) ──────────
kivy==2.2.0
kivymd==1.1.1

# ── App runtime dependencies ──────────────────────────────────
pyftpdlib==1.5.9
arabic-reshaper==3.0.0
python-bidi==0.4.2
plyer==2.1.0
